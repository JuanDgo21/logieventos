// Importamos íconos sólidos (de estilo sólido o 'solid') desde Font Awesome
import { 
  faHome,             // Icono de casa (inicio)
  faArrowRight,       // Flecha hacia la derecha
  faChevronDown,      // Flecha pequeña hacia abajo (usada para desplegables)
  faSignInAlt,        // Icono de iniciar sesión
  faUserPlus,         // Icono para registrar usuario nuevo

  faCheckCircle,      // Círculo con chulo (usualmente para indicar éxito)
  faStar,             // Estrella (para destacar o calificaciones)
  faBell,             // Campana (notificaciones)

  faCalendarDays,     // Calendario con días (fechas importantes)
  faCalendarCheck,    // Calendario con chulo (eventos confirmados)

  faUsers,            // Grupo de personas (usuarios)
  faUsersCog,         // Grupo de personas con engranaje (gestión de usuarios)
  faUserTie,          // Usuario con corbata (rol administrativo o ejecutivo)

  faBoxOpen,          // Caja abierta (inventario, productos)
  faBoxes,            // Varias cajas (almacén o stock)
  faLayerGroup,       // Capas (estructura o categorías)

  faFileContract,     // Documento con firma (contratos)
  faFileAlt,          // Documento (información o reportes)
  faChartPie,         // Gráfico circular (estadísticas)
  faChartLine,        // Gráfico de líneas (tendencias)

  faEnvelope,         // Sobre (correo electrónico)
  faPhone,            // Teléfono
  faMapMarkerAlt,     // Ubicación o dirección
  faTags,             // Etiquetas o categorías

  faPlayCircle,       // Botón de reproducir (videos)
  faRobot,            // Robot (automatización o IA)
  faRocket,           // Cohete (lanzamiento o innovación)
  faCogs,             // Engranajes (configuraciones)
  faClock,            // Reloj (tiempo o programación)
  faExpand,           // Expandir vista
  faMobileScreen      // Pantalla de celular (responsividad o apps móviles)
} from '@fortawesome/free-solid-svg-icons';

// Importamos íconos de marcas (logos de redes sociales o plataformas conocidas)
import {
  faTwitter,          // Logo de Twitter
  faFacebookF,        // Logo de Facebook
  faLinkedinIn,       // Logo de LinkedIn
  faInstagram,        // Logo de Instagram
  faGoogle,           // Logo de Google
  faMicrosoft,        // Logo de Microsoft
  faSlack             // Logo de Slack
} from '@fortawesome/free-brands-svg-icons';

// Exportamos todos los íconos que hemos importado, para usarlos fácilmente en otros archivos
export {
  // Íconos sólidos
  faHome,
  faArrowRight,
  faChevronDown,
  faSignInAlt,
  faUserPlus,
  faCheckCircle,
  faStar,
  faBell,
  faCalendarDays,
  faCalendarCheck,
  faUsers,
  faUsersCog,
  faUserTie,
  faBoxOpen,
  faBoxes,
  faLayerGroup,
  faFileContract,
  faFileAlt,
  faChartPie,
  faChartLine,
  faEnvelope,
  faPhone,
  faMapMarkerAlt,
  faTags,
  faPlayCircle,
  faRobot,
  faRocket,
  faCogs,
  faClock,
  faExpand,
  faMobileScreen,

  // Íconos de marcas
  faTwitter,
  faFacebookF,
  faLinkedinIn,
  faInstagram,
  faGoogle,
  faMicrosoft,
  faSlack
};
